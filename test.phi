;;let <set_name> := <expression>;
set A := R/{1}
 

;;let <variable_name>: <type_annotation> := <expression>;

let x: [a, b] := 42;

let a: {0, +inf} := 12;

let flag: bool := true;

let z: C := 3 + 2i;

